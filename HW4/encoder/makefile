CXX = g++
CXXFLAGS = -std=c++11 -g3
ENC_SOURCES = encoder.cpp
ENC_EXECUTABLE = encoder

all: $(ENC_EXECUTABLE)

$(ENC_EXECUTABLE): $(ENC_SOURCES)
	$(CXX) $(CXXFLAGS) $(ENC_SOURCES) -o $(ENC_EXECUTABLE)

clean:
	rm $(ENC_EXECUTABLE)
